version: 2

models:
  - name: stg_customers
    columns:
      - name:  customer_id
        tests:
          - not_null
          - unique
  - name: stg_orders
    columns:
      - name:  customer_id
        tests:
          -  relationships:
               field: customer_id
               to: ref('stg_customers')
